import { Loader } from "./loaderSpinner.styles";

/**
 * Primary UI component for user interaction
 */
const LoaderSpinner = () => {

  return (
      <Loader/>
  );
};

export default LoaderSpinner;
